import { ImagePreloader } from './image-preloader.directive';

describe('ImagePreloaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ImagePreloader();
    expect(directive).toBeTruthy();
  });
});
